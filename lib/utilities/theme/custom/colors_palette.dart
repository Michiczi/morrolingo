import 'package:flutter/material.dart';

class TColors {
  TColors._(); // Private constructor to prevent instantiation

  // App Basic Colors
  static const Color primary = Color(
    0xFF4B68FF,
  ); // Example primary color, can be changed
  static const Color secondary = Color(
    0xFFFFE24B,
  ); // Example secondary color, can be changed
  static const Color accent = Color(
    0xFFb0c7ff,
  ); // Example accent color, can be changed

  // Text Colors
  static const Color textPrimary = Color(
    0xFF333333,
  ); // Example text primary color, can be changed
  static const Color textSecondary = Color(
    0xFF6C757D,
  ); // Example text secondary color, can be changed
  static const Color textWhite = Colors.white;

  // Background Colors
  static const Color light = Color(
    0xFFF6F6F6,
  ); // Example light background color, can be changed
  static const Color dark = Color(
    0xFF272727,
  ); // Example dark background color, can be changed
  static const Color primaryBackground = Color(
    0xFFF3F5FF,
  ); // Example primary background color, can be changed

  // Background Container Colors
  static const Color lightContainer = Color(
    0xFFF6F6F6,
  ); // Example light container color, can be changed
  static const Color darkContainer = Color(
    0xFF272727,
  ); // Example dark container color, can be changed

  // Button Colors
  static const Color buttonPrimary = Color(
    0xFF4B68FF,
  ); // Example button primary color, can be changed
  static const Color buttonSecondary = Color(
    0xFF6C757D,
  ); // Example button secondary color, can be changed
  static const Color buttonDisabled = Color(
    0xFFC4C4C4,
  ); // Example button disabled color, can be changed

  // Border Colors
  static const Color borderPrimary = Color(
    0xFFD9D9D9,
  ); // Example border primary color, can be changed
  static const Color borderSecondary = Color(
    0xFFE6E6E6,
  ); // Example border secondary color, can be changed

  // Error and Validation Colors
  static const Color error = Color(
    0xFFD32F2F,
  ); // Example error color, can be changed
  static const Color success = Color(
    0xFF388E3C,
  ); // Example success color, can be changed
  static const Color warning = Color(
    0xFFF57C00,
  ); // Example warning color, can be changed
  static const Color info = Color(
    0xFF1976D2,
  ); // Example info color, can be changed

  // Neutral Shades
  static const Color black = Colors.black;
  static const Color darkerGrey = Color(
    0xFF4F4F4F,
  ); // Example darker grey color, can be changed
  static const Color darkGrey = Color(
    0xFF939393,
  ); // Example dark grey color, can be changed
  static const Color grey = Color(
    0xFFE0E0E0,
  ); // Example grey color, can be changed
  static const Color softGrey = Color(
    0xFFF4F4F4,
  ); // Example soft grey color, can be changed
  static const Color lightGrey = Color(
    0xFFF9F9F9,
  ); // Example light grey color, can be changed
  static const Color white = Colors.white;

  // Specific colors found in the project
  static const Color greenAccent = Colors.greenAccent;
  static const Color greenButtonBottom = Color(0xFF2E7D32);
  static const Color redButtonBottom = Color(0xFFC62828);
}
